export enum Gender {
  MALE,
  FEMALE,
}

export enum StudentStatus {
  ACTIVE,
  ALUMNI,
  DROPOUT,
  ACCOUNT_FREEZED,
  BLACKLISTED,
  SUSPENDED,
  DEACTIVATED,
}

export enum DeviceType {
  IOS,
  ANDROID,
  WEB,
  DESKTOP,
}

export enum EnrollmentStatus {
  PENDING,
  ACTIVE,
  COMPLETED,
  DROPPED,
  SUSPENDED,
  EXPELLED,
  TRANSFERRED,
  DEFERRED
}